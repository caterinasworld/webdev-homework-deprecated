const add = (a, b) => {
  // return the sum of the two parameters

  return a;
};

const subtract = (a, b) => {
  // return the subtraction result of the two parameters

  return a;
};

const multiply = (a, b) => {
  // return the multiplication result of the two parameters

  return a;
};

const divide = (a, b) => {
  // return the division result of the two parameters

  return a;
};

add(10, 5);
// 15
add(40, -15);
// 25

subtract(10, 5);
// 5
subtract(40, -15);
// 55

multiply(10, 5);
// 50
multiply(40, -15);
// -600

divide(10, 5);
// 2
divide(40, -15);
// -2.66 or -2.67
